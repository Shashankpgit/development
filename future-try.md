1. future make sure build a simple functionality which will use things like async, await and to understand . if see how it will work without this then how this keywords will help.
2. Even for the cors do this kind of thing.


Now introduce few more API's and we need to automate the infra creation using the iac in order to automate the process
We will build the infra using the opentofu and terragrunt. lets plan this and build this 

1. where the user can upload the files and document upto few 2mb
2. the user can download later
3. the user can view the uploaded file
4. The file or the documents should be stored in the buckets for the user safety, so that even though the cluster goes down the GKE craches we can recover.