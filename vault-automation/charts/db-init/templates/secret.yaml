# templates/secret.yaml
# Creates the K8s Secret holding the keycloak DB password.
# The Job reads from this Secret — password never appears in the Job spec.
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
type: Opaque
stringData:
  password: {{ .Values.dbInit.keycloakDbPassword | quote }}
