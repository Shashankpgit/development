# Secrets — never bake into the image
.env

# Virtual environment — installed fresh inside the container
.venv/
venv/

# Python cache — not useful inside the image
__pycache__/
*.pyc
*.pyo

# Test files — not needed in production image
tests/

# Docker files themselves
Dockerfile
.dockerignore
